package com.bookworm.application.customers.adapter.service.model

import com.bookworm.application.customers.domain.model.CustomerId

case class AuthenticationTerminationServiceModel(customerId: CustomerId)
