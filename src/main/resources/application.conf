server {
  host = "localhost"
  port = 8080
}

database {
  driver = "org.postgresql.Driver"
  url = "jdbc:postgresql://localhost:5432/Bookworm"
  user = "Bookworm"
  password = "password"
  thread-pool-size = 32
}

customer {
  verificationTokenExpirationInSeconds = 86400
}